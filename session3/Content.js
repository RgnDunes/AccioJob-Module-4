// Conditional Rendering + List & Keys attribute

// Inline if with Logical && operator
// true && expression -> expression
// false && expression -> false

// Inline if-else with conditional operator (ternary operator)
// condition ? statement1 : statement2

// Preventing Component from Rendering
// if the user is logged In display "Happy New Year" or else hide the component

// Rendering Multiple Components/elements (map)
